spring:
  application:
    name: day3-assignment

  datasource:
    password: 1
    url: jdbc:postgresql://localhost:2222/assignment3
    username: postgres

  jpa:
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always


  main:
    allow-bean-definition-overriding: true

#  web:
#    resources:
#      static-locations: classpath:/resources/images


server:
  port: 8080

